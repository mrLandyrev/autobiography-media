export type Theme = {
    colors: {
        primary: string,
        secondary: string,
        default: string,
    }
}

export const DefaultTheme: Theme = {
  colors: {
        primary: "#b42222",
        secondary: "#dc4747",
        default: "#414141ff",
  }
}